# Pin to the 1.x line: the high-level FastMCP API (mcp.server.fastmcp) is stable
# here. mcp 2.0.0 restructured it — do not unpin without adapting server.py.
mcp>=1.13,<2
uvicorn>=0.30.0
httpx>=0.27.0
